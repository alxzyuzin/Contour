#include "pch.h"
#include "LevelUnitTest.h"

using namespace Microsoft::VisualStudio::CppUnitTestFramework;
using namespace ContourExtractorWindowsRuntimeComponent;
using namespace std;

namespace ContourUnitTests
{
    TEST_CLASS(ContourUnitTests)
    {
    private:


    public:
        TEST_METHOD(Contour_FindInternalContour_1)
        {
            ; /* Contour contour;
            for (int i = 0; i < 8; i++)
            contour.AddPoint(Contour_1[i]);
            Assert::AreEqual(contour.Size(), 8);*/
        }
    };
}